# Docker部署专用环境变量配置
# 复制此文件为.env并修改相应的值

# 服务器配置
PORT=3000
NODE_ENV=production

# 安全配置（必须修改！）
# 使用以下命令生成随机密钥：
# Linux/Mac: openssl rand -hex 32
# Windows: 使用 https://randomkeygen.com/ 生成
ENCRYPT_SECRET=your-unique-encrypt-secret-key-must-be-changed-in-production
SESSION_SECRET=your-unique-session-secret-key-must-be-changed-in-production

# CORS配置
# 生产环境请修改为实际的域名
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# 请求限流配置
RATE_LIMIT_WINDOW_MS=900000    # 15分钟
RATE_LIMIT_MAX=100              # 最多100次请求

# 数据库配置（Docker内部路径）
DATABASE_PATH=/app/database.db

# 日志级别
LOG_LEVEL=info

# Docker健康检查配置
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3